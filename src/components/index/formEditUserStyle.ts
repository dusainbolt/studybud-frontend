import { Theme } from '@mui/material';
import { makeStyles } from '@mui/styles';

export const formEditUserStyle = makeStyles((theme: Theme) => ({
  main: {
    maxWidth: 500,
    marginBlock: theme.spacing(4),
  },
}));
